/** Admin award form uploads images via POST /api/upload. */
export const UPLOAD_CONTENT_API_ACTIONS = [
  "plugin::upload.content-api.upload",
  "plugin::upload.content-api.find",
  "plugin::upload.content-api.findOne",
] as const;
